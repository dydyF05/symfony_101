

{% block body %}
    <h1>Création d'article</h1>
    {{ form_start(form) }}
        {{form_row(form.Title)}}
        {{form_row(form.Content)}}
       
        {{form_rest(form)}}
        <input type="submit" value="send" />
    {{ form_end(form) }}
{% endblock %}