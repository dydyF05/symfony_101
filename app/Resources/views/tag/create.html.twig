{% block body %}
    <h1>Création de tag</h1>
    {{ form_start(form) }}
        {{form_row(form.Name)}}
       
        {{form_rest(form)}}
        <input type="submit" value="send" />
    {{ form_end(form) }}
{% endblock %}